# Copy to .env and fill in. The app reads these at startup.

# --- Claude / LLM -----------------------------------------------------------
# Required to generate answers (retrieval, PDFs, routing, and tests work without it).
ANTHROPIC_API_KEY=

# Required to use the /admin/* endpoints (sent as the X-Admin-Token header).
# If unset, the admin API is disabled (fail-closed) rather than left open.
ADMIN_TOKEN=
CLAUDE_MODEL=claude-opus-5          # or claude-sonnet-5 / claude-haiku-4-5 for cheaper traffic
CLAUDE_EFFORT=medium               # low | medium | high | xhigh | max
CLAUDE_MAX_TOKENS=2048
ENABLE_REFUSAL_FALLBACK=true

# --- Retrieval --------------------------------------------------------------
RETRIEVAL_TOP_K=4
CHUNK_SIZE_WORDS=220
CHUNK_OVERLAP_WORDS=40

# --- Freemium (Phase 4) -----------------------------------------------------
FREE_INQUIRIES_PER_MONTH=3

# --- Storage / runtime state ------------------------------------------------
# Defaults to ./var (sqlite db, generated PDFs, ingested docs). Encrypted EU volume in prod.
DATA_DIR=./var

# --- Weekly official-source ingestion (auto re-scan) ------------------------
# Opt-in: when enabled the app fetches the government sites in app/ingest/sources.py
# on a schedule, extracts text, and reloads the knowledge base with no restart.
INGEST_ENABLED=false
INGEST_INTERVAL_HOURS=168          # 168 = weekly
INGEST_ON_STARTUP=false            # also run once shortly after boot
INGEST_TIMEOUT_SECONDS=30
INGEST_USER_AGENT=CyprusCitizenAgent/1.0 (+https://example.com; knowledge-base refresh)

# --- WhatsApp Cloud API (Phase 2) -------------------------------------------
# From Meta for Developers -> your app -> WhatsApp product.
WHATSAPP_TOKEN=                    # permanent / system-user access token
WHATSAPP_PHONE_NUMBER_ID=          # the sending phone number id
WHATSAPP_VERIFY_TOKEN=             # any string you choose; used for webhook verification
WHATSAPP_APP_SECRET=               # app secret; used to verify inbound webhook signatures
WHATSAPP_GRAPH_VERSION=v21.0
# Public HTTPS URL of THIS service (only needed if you switch to sending media by link).
PUBLIC_BASE_URL=
