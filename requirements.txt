# Cyprus Bureaucracy & Citizen Agent — Q&A (RAG) MVP
# Runtime
anthropic>=0.49            # Claude API SDK (installs its own httpx2 backend)
fastapi>=0.110             # HTTP API
uvicorn[standard]>=0.29    # ASGI server
pydantic>=2.6              # models / validation (also pulled in by fastapi)
pydantic-settings>=2.2     # env-driven configuration

# Dev / test
pytest>=8.0
httpx>=0.27               # required by fastapi's TestClient (Starlette)
