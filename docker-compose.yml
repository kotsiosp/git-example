services:
  agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    volumes:
      - agent-data:/data
    restart: unless-stopped

volumes:
  agent-data:
